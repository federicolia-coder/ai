/** Public address of the site, used for canonical links, the sitemap and social previews. */
export const SITE_URL = (process.env.NEXT_PUBLIC_APP_URL || "https://tarry-web.onrender.com").replace(/\/$/, "");
